export enum HttpCodes {
    OK = 200,
    Created = 201,
    Bad_Request = 400,
    Unauthorized = 401,
    WrongPassword = 402, // This is not an official code.
    // Forbidden = 403,
    Not_Found = 404,
    // Request_Timeout = 408,
    Conflict = 409,
    Internal_Error = 500,
}
